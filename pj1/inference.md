## 推理

#### part1 的分类任务: 

```
# 在根目录下
cd part1
python interview_classification.py
```

#### part2 的分类任务: 

```
# 在根目录下
cd part2
python interview_cnn.py
```



## 如需要训练并重新生成参数

#### 运行 part1 的 `regression.py` / `regression_v2.py`:

其中 `regression_v2.py` 在 `regression.py` 的基础上增加了拓展性，其效果更好。

```
# 在根目录下
cd part1
python regression_v2.py
```

#### 运行 part1 的 `classification.py`:

```
# 在根目录下
cd part1
python classification.py
```

#### 运行 part2 的 `CNN.py`:

```
# 在根目录下
cd part2
python CNN.py
```